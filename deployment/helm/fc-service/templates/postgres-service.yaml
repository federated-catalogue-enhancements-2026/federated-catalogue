apiVersion: v1
kind: Service
metadata:
  name: fc-postgres
  namespace: {{ .Release.Namespace }}
  labels:
    app: fc-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: fc-postgres
