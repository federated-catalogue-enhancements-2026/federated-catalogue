apiVersion: v1
kind: Service
metadata:
  name: fc-demo-portal
  namespace: {{ .Release.Namespace }}
  labels:
    app: fc-demo-portal
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8088
      targetPort: 8088
  selector:
    app: fc-demo-portal
